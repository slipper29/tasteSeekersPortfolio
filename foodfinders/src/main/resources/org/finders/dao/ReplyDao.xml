<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.finders.dao.ReplyDao">
	<select id="overlapId" resultType="org.finders.model.ReplyDTO">
		SELECT *
		FROM mas_member
		where id=#{id}
	</select>
</mapper>